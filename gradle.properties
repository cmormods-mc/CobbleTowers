org.gradle.jvmargs=-Xmx2G -XX:MaxMetaspaceSize=1G -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true

# The CobbleRaids build this compiles against. Published to Maven Local from SnobblemonRaids with
# `gradlew publishToMavenLocal`; bump both together when the encounter API changes.
cobbleraids_version=0.8.95-encounter-rules
