package com.cobbletowers.api.tower.participant;

/** Whether a participant is on the server. Independent of what they may do in the run. */
public enum ConnectionState {
    ONLINE,
    DISCONNECTED
}
